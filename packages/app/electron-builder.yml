appId: com.areco.app
productName: areco
directories:
  buildResources: build
  output: dist
files:
  - main.js
  - preload.js
  - offline.html
  - build/icon.png
mac:
  category: public.app-category.utilities
  icon: icon.icns
  notarize: false
  extendInfo:
    CFBundleDisplayName: areco
dmg:
  artifactName: ${name}-${version}-${arch}.${ext}
